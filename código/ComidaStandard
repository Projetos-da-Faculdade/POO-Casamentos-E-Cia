using System;
using System.IO;

internal class ComidaStandard : Comida
{
    public override double DefinirPrecoComida(int quantidadeConvidados)
    {
        return quantidadeConvidados * 60;
    }
}
