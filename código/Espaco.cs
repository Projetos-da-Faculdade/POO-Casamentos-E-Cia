using System;

class Espaco{
    //Atributos
    private const int A = 100, B = 100, C = 100, D = 100, E = 200, F = 200, G = 50, H = 500;
    private bool espacoDisponivel;

    //Método 
    //Substitua "throw new NotImplementedException();" pelo código correto. 
    public void MudarDisponivel(bool espacoDisponivel){
        throw new NotImplementedException();
    } 
}